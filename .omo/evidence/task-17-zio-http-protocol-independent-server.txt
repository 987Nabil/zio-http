# Todo 17 — Stress lifecycle, isolation, and resource cleanup — evidence

Workspace: /home/user/worktrees/zio-http-task17-stress (jj workspace, parent zwpolmsl = Todo 13 tip)
Change: one new file only — zio-http-server-loom/src/test/scala/zio/http/MultiProtocolStressSpec.scala
  (deterministic bounded stress/resource diagnostics fixtures; no prod-code changes)

## Scope (Todo 17 exact)
6 latch/barrier-bounded tests, no sleep-polling:
1. repeated serve/stress/shutdown cycles (3x) — no listener/connection residue accumulation
2. malformed client storm on shared cleartext dispatch (garbage / bare CRLF /
   diverged truncated preface / TE+CL / control-char header) — no poisoning of later requests
3. slow-trickle POST bodies + abrupt RST + stale half-open headlines — settle and drain cleanly
4. shutdown race: parked in-flight H1+H2 pair + 8 concurrent shutdown callers + real-await waiter
5. peer engine failure isolation (failing LifecycleEngine peer) + 2x bind-collision rollback/recover rounds
6. shared H1H2CleartextEngine under concurrent mixed H1/H2 load (40 req) — all serve, clean shutdown
Diagnostics asserted every test: awaitShutdown truly blocks (early==false waiter latch),
awaitQuiescent true on every engine, TCP refused on every port, handle terminated.

## Baseline (pre-change)
./mill serverLoom.jvm.3_9_0.test.testOnly zio.http.AggregateServerIntegrationSpec -- -t "one serve call ..."
-> compile of untouched tree OK; integration spec green (re-confirmed in combined run below).

## RED proof (failing stress, then fix)
Full-spec run 1: 5 passed, 1 failed —
  "shutdown race ..." : loadOk.get() == 4 failed with 2 (load raced stop-accepting; refusals are legal).
Fix (test-only): sequence the 4-client /ok load to complete before the shutdown race starts.
Full-spec run 2: 6 passed, 0 failed.

## Repeated bounded run + regressions (final)
./mill serverLoom.jvm.3_9_0.test.testOnly zio.http.MultiProtocolStressSpec \
  zio.http.AggregateServerIntegrationSpec zio.http.MultiConnectorIntegrationSpec \
  zio.http.AggregateLifecycleSpec
-> 38 tests passed, 0 failed (6 stress + 10 aggregate-integration + 7 multi-connector + 15 lifecycle).
Stress spec executed 3x total (isolated 1-test run, full run, combined run) — stable, no flakes.

## Format / diagnostics
./mill mill.scalalib.scalafmt/reformatAll -> SUCCESS (only the new spec file in the change).
lsp_diagnostics (error) on MultiProtocolStressSpec.scala -> clean.

## Commit
test(server): stress multi-protocol lifecycle (jj, single atomic change on Todo 13 tip).
No push/PR without explicit permission. No docs/build/perf/parser scope touched.

## Delivery DoneClaim (PR #22)
- PR: https://github.com/987Nabil/zio-http/pull/22 (base task13-multi-connector <- task17-stress-lifecycle), OPEN, unmerged.
- Push: bookmark task17-stress-lifecycle -> fork, commit e3cb873c, GPG verified=true reason=valid.
- CI repair: 2.13 job first failed on Scala-3-only rename import (fixed test-only, cross-version braces syntax; local 2.13 test.compile SUCCESS).
- Adversarial flake audit: second 2.13 failure was H2IntegrationSpec "multiple routes dispatch" RstStream(3,Code(8)) CANCEL — pre-existing server test untouched by this change; matches known H2 loaded-host RST flake. Local 2.13 rerun of the exact test PASSED (454ms); CI --failed rerun then went fully green.
- Final checks: build SUCCESS, Lint SUCCESS, JVM 2.13.18 SUCCESS, JVM 3.9.0 SUCCESS, website SUCCESS, CI SUCCESS. No reviews/comments. Workspace clean (empty @).
